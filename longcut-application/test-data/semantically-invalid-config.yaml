#### The core configuration declares a parameter without a type.

gui:
  font_family: "sans-serif"
  font_size: 18
  window_width: 1200
  window_height: 300

shell:
  default_timeout_ms: 5000

core:
  commands:
    - name: Example
      steps: []
      parameters:
        - name: Player name